{
  "name": "task-management-app",
  "version": "1.0.0",
  "description": "Full-stack task management application with Node.js backend and React frontend",
  "main": "backend/server.js",
  "scripts": {
    "start": "cd backend && npm start",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm start\"",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "test": "cd backend && npm test",
    "deploy": "./deploy.sh"
  },
  "keywords": [
    "task-management",
    "nodejs",
    "react",
    "express",
    "fullstack"
  ],
  "author": "Your Name",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
